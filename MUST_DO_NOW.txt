╔════════════════════════════════════════════════════════════╗
║                  ⚠️  CRITICAL INSTRUCTIONS                 ║
║                  FOLLOW EXACTLY OR WON'T WORK              ║
╔════════════════════════════════════════════════════════════╗

STEP 1: KILL ALL NODE PROCESSES
================================
Run this command in terminal:

    pkill -9 node

Wait 3 seconds.


STEP 2: CLEAR NODE CACHE (IMPORTANT!)
=====================================
Run these commands:

    rm -rf node_modules/.vite
    rm -rf dist


STEP 3: START FRESH DEV SERVER
===============================
Run:

    npm run dev


STEP 4: OPEN BROWSER
====================
Go to: http://localhost:5173


STEP 5: CHECK CONSOLE
=====================
Press F12 to open Developer Console.

You MUST see:
    🔍 Environment Variables Check:
    POE: ✅
    TOGETHER: ✅
    GROQ: ✅
    ✅ GROQ KEY LOADED: gsk_YbCjZooAS...

If you see:
    ❌ GROQ KEY NOT LOADED!

Then you need to:
    1. Stop server (Ctrl+C)
    2. Run: pkill -9 node
    3. Run: npm run dev again


STEP 6: TEST MODEL SELECTOR
============================
1. Click "Browse All Models (11)"
2. You should see 3 TABS:
   - Poe (2)
   - Together AI (4)  
   - Groq (5) ← THIS MUST APPEAR!

If Groq tab NOT showing:
   - Check console for GROQ: ❌
   - Server was NOT restarted properly
   - Go back to STEP 1


╔════════════════════════════════════════════════════════════╗
║  IMPORTANT: VITE ONLY READS .env ON SERVER START          ║
║  YOU MUST KILL AND RESTART - NOT JUST Ctrl+C!             ║
╚════════════════════════════════════════════════════════════╝

FULL COMMAND SEQUENCE:
======================

pkill -9 node
rm -rf node_modules/.vite dist
npm run dev

Then open browser and check console!

╚════════════════════════════════════════════════════════════╝
